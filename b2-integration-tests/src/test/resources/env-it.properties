# environment variables
env.JAVA_HOME=${java.home}
env.M2_HOME=${it.dir}/apache-maven-${maven.version}
env.MAVEN_OPTS=-Xms64m -Xmx128m -Duser.home="${it.user.home}"
env.PATH=${java.home}/bin${path.separator}${env.PATH}

# system properties
it.resources=${project.build.testOutputDirectory}
build.dir=${it.dir}
user.home=${it.user.home}
java.home=${java.home}
debug.allowed=${it.debugAllowed}
http.proxyHost=${http.proxyHost}
http.proxyPort=${http.proxyPort}
http.nonProxyHosts=${http.nonProxyHosts}